module.exports = {
  // Set this to the exact name of your WhatsApp group
  TARGET_GROUP_NAME: "Geeta Daily",

  // Daily send time (cron format: minute hour * * *)
  SCHEDULER_CRON: "0 5 * * *", // 5:00 AM IST

  TIMEZONE: "Asia/Kolkata",
};
